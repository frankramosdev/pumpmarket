import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Nothing unusual — defaults work well in Next 16.
  // Turbopack is on by default for dev and build.
};

export default nextConfig;
